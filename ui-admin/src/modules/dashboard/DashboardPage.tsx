import { Card, CardContent, Typography } from "@mui/material";

export const DashboardPage = () => (
  <Card>
    <CardContent>
      <Typography variant="h5">Dashboard</Typography>
      <Typography variant="body2">
        Bem-vindo ao sistema de gestão de filas!
      </Typography>
    </CardContent>
  </Card>
);
